{
  "name": "robotframework-containers",
  "version": "0.0.0",
  "private": true,
  "description": "Tooling-only manifest: pins the Prettier version used to format this repository.",
  "license": "MIT",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "3.9.7"
  }
}
